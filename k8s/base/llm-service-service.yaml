apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: rag-chatbot
  labels:
    app: llm-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-service 