apiVersion: v1
kind: Service
metadata:
  name: vector-db
  namespace: rag-chatbot
  labels:
    app: vector-db
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: vector-db 